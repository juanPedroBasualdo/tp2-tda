\documentclass[12pt]{article}
\usepackage{graphicx}
\usepackage[utf8]{inputenc}
\usepackage{textcomp}
\usepackage[spanish]{babel}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{fancyvrb}


\definecolor{comment}{RGB}{127, 127, 127}

\lstdefinestyle{pseudo}{
    basicstyle=\ttfamily\footnotesize,
    breaklines=true,
    keepspaces=true,
    frame=single,
    captionpos=b,
    rulecolor=\color{gray},
    moredelim=[s][\color{comment}\bfseries]{\#}{.}
}

\begin{document}

\begin{titlepage}
    \centering
    \vspace*{1cm}
    
    % Logo
    \includegraphics[width=0.2\textwidth]{imagenes/logo.png}\par\vspace{0.5cm}
    
    % Título
    {\scshape\Large Teoría de Algoritmos (TB024/75.29)\par}
    \vspace{1.5cm}
    {\LARGE Trabajo Práctico N° 2\par}
    {\huge\bfseries Redes de flujo, Clases de Complejidad y Fuerza Bruta\par}
    \vspace{2cm}
    
    % Autor
    {\Large Grupo 3SAT \par}
    \vfill
    
    % Profesor
    Profesor:\par
    Ing. Victor Podberezky\par
    \vfill
    
    % Fecha
    {\large \today\par}
\end{titlepage}

\tableofcontents
\newpage

\section{Introducción}

    El estudio avanzado de algoritmos permite no solo resolver problemas de manera eficiente, sino también comprender las limitaciones intrínsecas de la computación y la estructura subyacente de problemas complejos de optimización. Mientras que las técnicas de diseño clásicas proporcionan herramientas para construir soluciones, el análisis de flujos en redes y la clasificación de problemas según su complejidad permiten determinar la viabilidad de dichas soluciones en entornos reales. 
    
    En el presente informe, se profundizará en el estudio de tres ejes fundamentales de la algoritmia moderna: la modelización mediante redes de flujo, la implementación de estrategias de búsqueda exhaustiva y el marco teórico de las clases de complejidad.En la primera parte de este trabajo, se abordará la teoría de Redes de Flujo, una herramienta esencial para resolver problemas de transporte, logística y asignación de recursos. Se analizará cómo modelar un sistema de capacidades para determinar el flujo máximo posible entre nodos, aplicando conceptos como el teorema de Max-Flow Min-Cut. 
    
    Posteriormente, se explorará la técnica de Fuerza Bruta, evaluando su utilidad como punto de partida para la resolución de problemas combinatorios y discutiendo el costo computacional que implica la exploración completa del espacio de soluciones. 
    
    Finalmente, se presentará un análisis sobre las Clases de Complejidad, estableciendo las distinciones fundamentales entre problemas de las clases \textit{P}, \textit{NP} y \textit{NP-completos}. Este apartado permitirá comprender la frontera entre lo tratable y lo intratable, brindando los criterios necesarios para identificar cuándo un problema exige una solución heurística o aproximada frente a la imposibilidad de hallar una solución exacta en tiempo polinomial.

\section{Desarrollo del trabajo}

    \subsection{La separación del Dojo}

        Las redes de flujo permiten modelar una gran variedad de problemas. Es posible representar cada arista dirigida como un conducto con capacidad limitada para transportar un fluido, y cada vértice como una intersección de dichos conductos. En consecuencia, por la ley de conservación, la cantidad de fluido que sale de cada intersección debe ser igual a la que ingresa. Gracias a esta relación de equilibrio, es posible identificar características que permiten resolver problemas complejos mediante una reducción polinomial a una red de flujo.

        El conocimiento sobre el comportamiento del flujo, el valor máximo que puede alcanzar la red y la identificación del ``cuello de botella'', determinado por el corte que minimiza la capacidad entre la fuente y el sumidero, es lo que permite adaptar este modelo a problemas que, a simple vista, no parecen tener una estructura de red. De esta forma, se logran soluciones algorítmicas eficientes para desafíos de optimización y particionamiento.

        \subsubsection{Explicación del problema}

            Un dojo tiene ``n'' miembros, dos de ellos son profesores, cada uno de los miembros del dojo se relacionó al menos una vez con alguno de los profesores. Dado un grupo de relaciones $R_{A}$, cuyos elementos son tuplas del tipo $[(m_{i}, m_{j}), r_{ij}]$ donde $m_{i}$ y $m_{j}$ son miembros del dojo tal que $m_{i} \ne m_{j}$ y $r_{ij}$ es un entero positivo representativo de las veces que se relacionó el miembro $m_{i}$ con $m_{j}$, se busca dividir a los miembros del dojo entre los dos profesores de forma que la cantidad de veces que se relacionaron dos miembros de un grupo sea la máxima posible. 

            \textbf{Supuestos del problema:} Se supone que cada miembro del dojo se relacionó al menos una vez con alguno de los profesores, y que en el grupo de relaciones no pueden haber relaciones negativas. Además se supone que el primer profesor $p_1$ tiene número de miembro 1 y el profesor $p_2$ tiene numero de miembro ``n''.

        \subsubsection{Propuesta de solución por red de flujos}

            Para resolver el problema de la separación del Dojo asegurando que la suma de las intensidades de las relaciones rotas sea mínima, modelamos la situación mediante una red de flujo $G = (V, E)$ con capacidades asociadas a las aristas. El objetivo principal consiste en hallar un corte mínimo que separe a los dos profesores rivales ($p_1$ y $p_2$).

            A continuación, se detalla el diseño y la estructura formal de la red de flujo propuesta:

        \subsubsection{Modelado de Nodos ($V$)}
            El conjunto de vértices $V$ de la red representa a la totalidad de los integrantes del dojo. Cada profesor o maestro se corresponde con un nodo único dentro del grafo. Adicionalmente, se identifican de manera fija dos nodos especiales que actúan como terminales de la red:
            
            \begin{itemize}
                \item \textbf{Fuente ($s$):} Corresponde al Profesor 1 ($p_1$), quien encabezará el primer subgrupo.
                \item \textbf{Sumidero ($t$):} Corresponde al Profesor 2 ($p_2$), quien encabezará el segundo subgrupo enfrentado.
            \end{itemize}

        \subsubsection{Modelado de Aristas ($E$) y Capacidades ($c$)}

            Dado que las relaciones de afinidad o tensión entre los miembros del dojo son simétricas y recíprocas (es decir, si el profesor $u$ tiene un vínculo de peso $w$ con el profesor $v$, el profesor $v$ posee inherentemente el mismo grado de vínculo con $u$), \textbf{las relaciones deben modelarse de forma bidireccional}. 


            Para representar correctamente esta naturaleza no dirigida dentro de una red de flujo (la cual es por definición dirigida), cada relación de peso $w$ provista en el enunciado entre un par de nodos $u$ y $v$ se traduce en \textbf{dos aristas dirigidas simétricas} en el grafo:

            \begin{itemize}
                \item Una arista dirigida $(u, v)$ con capacidad inicial $c(u, v) = w$.
                \item Una arista dirigida $(v, u)$ con capacidad inicial $c(v, u) = w$.
            \end{itemize}

            En caso de existir múltiples especificaciones de relaciones o vínculos superpuestos entre el mismo par de nodos en el conjunto de datos de entrada, las capacidades correspondientes se acumulan de forma aditiva, garantizando la consistencia del modelo:
            \[
            c(u, v) = c(v, u) = \sum w_{uv}
            \]

        \subsubsection{Justificación del enfoque por Corte Mínimo}

            Al aplicar el teorema de \textit{Max-Flow Min-Cut} (Flujo Máximo - Corte Mínimo), sabemos que el valor del flujo máximo que puede transportarse desde la fuente $s$ hasta el sumidero $t$ es exactamente igual a la capacidad del corte mínimo que separa a ambos nodos en dos subconjuntos disjuntos de vértices, $S$ (donde $s \in S$) y $T$ (donde $t \in T$). 

            Las aristas que atraviesan este corte (es decir, aquellas aristas dirigidas que van desde el subconjunto $S$ hacia el subconjunto $T$ y cuyas capacidades residuales quedan completamente saturadas en $0$ al finalizar la ejecución del algoritmo) representan con precisión matemática los vínculos de afinidad que deben disolverse. De este modo, la partición de la red en los componentes disjuntos $S$ y $T$ determina de manera óptima la conformación de los dos nuevos dojos independientes, minimizando estrictamente el costo total de las relaciones rotas. 
        
        \subsubsection{Pseudocódigo}
        
            Una posible implementación del código solución de esta propuesta puede ser el siguiente:

            \begin{lstlisting}[style=pseudo,caption={Pseudocódigo de la solución}]
function definirGruposDojo(miembros V, relaciones R, p1, p2):
    G = Grafo nuevo
    
    for cada relacion (u, v, peso) in R:
        G.agregarVertice(u)
        G.agregarVertice(v)
        
        G.capacidad[u][v] = G.capacidad[u].get(v, 0) + peso
        G.capacidad[v][u] = G.capacidad[v].get(u, 0) + peso

    s = p1
    t = p2

    # Obtenemos el grafo residual tras aplicar Edmonds-Karp.
    G_res = EdmondsKarp(G, s, t)

    grupo_p1 = {s}
    cola = [s]

    while cola no esta vacia:
        u = cola.desencolar()
        for cada vecino v de u en G_res:
            # Solo avanzamos si hay capacidad residual > 0.
            if v no esta en grupo_p1 y G_res.capacidad[u][v] > 0:
                grupo_p1.agregar(v)
                cola.encolar(v)

    grupo_p2 = G.V - grupo_p1

    return grupo_p1, grupo_p2
            \end{lstlisting}

            Donde la función \texttt{FlujoMaximo} está determinada por el siguiente Pseudocódigo: 

            \begin{lstlisting}[style=pseudo,caption={Pseudocódigo de FlujoMaximo}]
function EdmondsKarp(Grafo G, s, t):
    # Inicialmente el grafo residual es igual al original.
    G_res = copiar(G) 
    flujo_total = 0

    camino = BFS_camino_aumento(G_res, s, t)

    while camino existe:
        flujo_camino = minimo_cuello_botella(camino)

        for cada arista (u, v) en camino:
            G_res.capacidad[u][v] -= flujo_camino  # Restamos flujo a la arista directa.
            G_res.capacidad[v][u] += flujo_camino  # Sumamos flujo a la arista inversa.

        flujo_total += flujo_camino
        camino = BFS_camino_aumento(G_res, s, t)

    return G_res
            \end{lstlisting}

        \subsubsection{Análisis de la solución}

            A partir del pseudocódigo presentado anterior se puede realizar un análisis en profundidad de la solución propuesta, comenzando por un análisis de uso de recursos de tiempo y espacio, para luego terminar de definir detalles que pueden haber quedado sin analizar.

            \textbf{Análisis de eficiencia espacial:} En cuestiones de uso de espacio, este algoritmo ocupa distintos ordenes de complejidad según como se implemente el grafo a utilizarse. En caso de usar una lista de adyacencia, la complejidad espacial de guardar el grafo de relaciones conllevaría $O(V+E)$, mientras que una matriz de adyacencias utiliza $O(V^2)$. Dado que en este trabajo se implementaron las soluciones algorítmicas en el lenguaje de programación Python, se utilizó la lista de adyacencias\footnote{En este caso sería más adecuado decir ``Diccionario de adyacencias''.} y por lo tanto, en consecuencia a este hecho, la complejidad espacial del algoritmo será igual a $O(V+E)$. Pues guardar todos los integrantes requiere $n$ vértices o también $O(V)$ y guardar cada una de las aristas requiere $m$ aristas para una dirección de las relaciones y $m$ aristas para la otra, lo que es lo mismo que $O(2E)$. Esto resulta en la ecuación de complejidad espacial: 
            \begin{center}
                $O(V) + O(2E) = O(V+2E) = O(V+E)$
            \end{center}

            Cabe aclararse adicionalmente, que la operatoria de búsqueda de flujo máximo requiere $O(V)$ espacio adicional, que por sumarse a la complejidad mencionada anteriormente no afecta al requerimiento espacial final ya que $O(V) \subset O(V+E)$.
            
            \textbf{Análisis de eficiencia temporal:} Respecto al tiempo de ejecución del algoritmo, construir la red de relaciones lleva $O(V+E)$ en ordenes de tiempo e identificar los distintos subconjuntos también requieren $O(V+E)$. Dado que se eligió el algoritmo de Edmonds-Karp, este tiene una complejidad de $O(V+E^{2})$ dado que iterando por cada arista ($O(E)$) se busca un camino de aumento ($O(V+E)$) lo que resulta en la complejidad dada. Adicionalmente, contabilizar cuantos miembros contienen cada subconjuntos puede conllevar como mucho una complejidad de $O(V)$. 
            
            Ya que $O(V) \subset O(V+E)$ y $ O(V+E) \subset O(V+E^{2})$, la complejidad resulta:
            \begin{equation}
                \centering 
                2O(V+E)+O(V+E^{2})+O(V)=O(V+E^{2})
            \end{equation}
            
            \textbf{Otros detalles de la solución:} En esta subsección del análisis de la propuesta algorítmica se busca responder a las preguntas ``\textit{¿Realmente la propuesta predicirá la división final?}'' y ``\textit{¿Puede una misma instancia tener diferentes soluciones? ¿Que implicación tiene elegir una solución por encima de otra?}''.
            
            Respecto a la primer pregunta, esta solución no predicirá la división final entre los miembros, sino que esta solución presenta un modelo de estimación simplificada. Esto está dado porque el corte mínimo asume que el sistema busca el estado de menor fricción, es decir la separación del grupo de miembros del dojo que mantenga los vínculos más fuertes. Adicionalmente, dado que es un modelo estimativo, puede ocurrir en la realidad que por fuera de toda estimación dada por esta solución, un alumno elija irse con un profesor en vez del estimado. Esta variación además podría generar un efecto cascada de cambio de profesor de alumnos de relación fuerte con el miembro que eligió al otro profesor.

            Otra característica de esta solución es el hecho de que pueden haber múltiples opciones de corte en una misma instancia. En una red de flujo, el flujo máximo toma un único valor, pero el corte mínimo de capacidades de dicha red no tiene porque ser uno solo. Tomando el caso donde hay un solo miembro además de ambos profesores y que dicho miembro interactuó una misma cantidad de veces con ambos profesores, ocurre el caso donde la red de flujo tendrá un flujo máximo igual al número de veces que haya interactuado el alumno con los profesores pero el corte que minimiza las capacidades puede realizarse desde la fuente o desde el sumidero, por lo que dará distintas soluciones según como se haya determinado la operación de corte.

            \begin{figure} [ht]
                \centering
                \includegraphics[width=0.8\textwidth]{imagenes/edge-1.png}\label{edge-1}
                \caption{Caso borde donde se pone en duda donde se realizará el corte mínimo}
            \end{figure}

            Este problema de incertidumbre es equivalente para cualquier cantidad de miembros, siempre que se cumpla que ambos profesores hayan interactuado la misma cantidad de veces con ellos y no hayan interacciones entre miembros. Consecuentemente, si bien el Teorema de Flujo Máximo-Corte Mínimo ofrece una solución óptima en términos de capacidad, la existencia de múltiples cortes mínimos sugiere que ciertos miembros se encuentran en una posición de equilibrio social. La elección de la fuente ($s_1$) para iniciar la exploración del grafo residual inclina la asignación de estos miembros en disputa hacia el primer profesor.

        \subsubsection{Ejemplo gráfico de ejecución de la solución}

            En esta sección se mostrará la solución de una instancia del problema según el algoritmo mostrado anteriormente. Dada la instancia siguiente instancia: 
            
            $[([1,2],4),([1,3],4),([1,5],1),([2,5],1),([5,7],5),([3,4],4),([5,6],5),$
            
            $([6,7],5), ([4,5],1)]$
            
            Con el profesor 1 representado por el número 1 y el profesor 2 representado por el número 7. Se construye una red de flujo a partir de ella, donde los nodos fuente y sumidero son el profesor 1 y el profesor 2 respectivamente, los nodos intermedios son los miembros del dojo y las aristas dirigidas y ponderadas son las veces que interactuaron dos miembros. La instancia elegida para este ejemplo conforma la red mostrada en la Figura 4.

            \begin{figure} [ht]
                \centering
                \includegraphics[width=0.8\textwidth]{imagenes/ej-1.png}\label{ej1}
                \caption{Red conformada por la instancia dada.}                
            \end{figure}

            En una primera iteración, el algoritmo busca el camino más corto entre $p_1$ y $p_2$ (marcada en violeta) y busca el cuello de botella de dicho camino para poder saturar sus aristas, tanto de una dirección como de otra. En este caso como el cuello de botella es 1, se pasa una unidad de flujo, restando en una dirección y sumando en la otra.

            \begin{figure} [ht]
                \centering
                \includegraphics[width=0.8\textwidth]{imagenes/ej-1-acumulado.png}
                \label{acumulado1}
                \caption{Red con 1 unidad de flujo acumulada.}                
            \end{figure}

            Esta operatoria se repite hasta que no existan más caminos que conecten $p_1$ con $p_2$. Cuando esto ocurra, el flujo máximo de la red será la cantidad de unidades de flujo acumuladas tras la saturación de aristas. 

            \begin{figure} [ht]
                \centering
                \includegraphics[width=0.8\textwidth]{imagenes/ej-1-acumulados.png}
                \label{acumulado2}
                \caption{Red sin caminos $p_1 \rightarrow p_2$, con 4 unidades de flujo acumulado}
            \end{figure}

            En este ejemplo se encuentra en una segunda iteración el camino $p_1 \rightarrow 2 \rightarrow 5 \rightarrow p_2$ cuyo cuello de botella es la arista $(2\rightarrow5)$ y su complemento en la otra dirección, con una capacidad de 1. En una tercera operación, se satura la arista $(3\rightarrow6)$ de capacidad 1, correspondiente al camino $p_1\rightarrow3 \rightarrow6 \rightarrow p_2$. Y por último se encuentra el camino más corto $p_1 \rightarrow 3 \rightarrow 4 \rightarrow 5 \rightarrow p_2$ que encuentra el cuello de botella en $(3\rightarrow4)$ de capacidad 1.

            \begin{figure} [ht]
                \centering
                \includegraphics[width=0.8\textwidth]{imagenes/ej-1-cut.png}
                \label{ej1-cut}
                \caption{Corte mínimo de la instancia.}                
            \end{figure}

            Entonces a partir de estos pasos realizados, el corte mínimo que particiona a la red de forma que se minimice la cantidad de relaciones perdidas tras la separación sera el corte que atraviese aquellas aristas que tras saturarse resultan tener capacidad 0.

            \begin{figure} [ht]
                \centering
                \includegraphics[width=0.8\textwidth]{imagenes/ej-1-resultado.png}
                \label{ej1-resultado}
                \caption{Grupos resultado de la instancia}
            \end{figure}

            Una vez logrado saturar las aristas de la red, se pueden obtener ambos grupos separando aquellos nodos que aún son alcanzables por $p_1$, en este ejemplo $\{2, 3, 4\}$, de los que no son alcanzables  $\{5, 6\}$

    \subsection{El despacho de los productos}

        En muchos problemas de logística y optimización de recursos no solo importa maximizar una ganancia, sino también minimizar las pérdidas asociadas al desperdicio de capacidad o recursos no utilizados. Este tipo de situaciones aparece frecuentemente en problemas de empaquetado, transporte y asignación de tareas, donde encontrar una solución óptima requiere analizar múltiples configuraciones posibles.

        En esta sección se resolverá un problema de optimización mediante la técnica de \textit{Branch \& Bound}. Esta técnica puede entenderse como una mejora sobre la búsqueda exhaustiva tradicional, ya que mantiene la optimalidad de fuerza bruta, pero reduce considerablemente la cantidad de soluciones analizadas gracias al uso de podas. 

        \subsubsection{Explicación del problema}

            Se tiene un conjunto de productos donde cada producto \(i\) está representado por la tupla \((s_i, v_i)\), donde \(s_i\) representa el tamaño del producto y \(v_i\) el valor obtenido por enviarlo. Además, se tiene \(k\) contenedores iguales, cada uno con capacidad máxima \(C\), y un factor de penalización \(a\) aplicado sobre el espacio libre de los contenedores que hayan sido utilizados. Cada producto puede colocarse en un único contenedor o no enviarse. A su vez, la suma de los tamaños de los productos que se encuentran dentro de un mismo contenedor no puede superar su capacidad máxima.

            El objetivo consiste en maximizar la ganancia total obtenida por el envío de productos, teniendo en cuenta además la penalización por espacio desaprovechado. Esto esta dado por:

            \[
            G = \sum v_i - a \cdot \sum espacio\_libre
            \]

        \subsubsection{Búsqueda exhaustiva sin poda}

            Una solución al problema consiste en realizar una búsqueda exhaustiva de todas las configuraciones posibles. Para cada producto existen \(k+1\) decisiones posibles, ya que este puede colocarse en cualquiera de los \(k\) contenedores disponibles o directamente no enviarse. Por lo tanto, si existen \(n\) productos, el número total de combinaciones posibles que podrían generarse es:

            \[
            (k+1)^n
            \]

            Esta manera garantiza encontrar la solución óptima ya que analiza todas las combinaciones válidas posibles. Sin embargo, es ineficiente debido a que incluso las ramas que claramente no superan la mejor solución encontrada continúan explorándose hasta el final. Para evitar este problema, se agrega una función límite que permite estimar si una rama todavía tiene posibilidades de mejorar la solución óptima actual y, en caso contrario, descartarla antes de recorrerla completamente.

        \subsubsection{Propuesta}

            Se propone construir un árbol de decisiones donde cada nivel representa un producto distinto. Para cada producto \(i\) se generan hasta \(k+1\) ramas posibles, correspondientes a colocarlo en alguno de los \(k\) contenedores disponibles o directamente no enviarlo. De esta manera, cada nodo del árbol mantiene el estado parcial de la solución construida hasta el momento.

            Durante el recorrido del árbol, el algoritmo calcula una estimación del máximo beneficio que todavía podría alcanzarse usando los productos restantes. Si esta no supera la mejor solución encontrada hasta el momento, entonces la rama actual se poda y deja de explorarse, evitando así recorrer nodos que no conducen a una solución óptima.

        \subsubsection{Explicación del funcionamiento}

            El algoritmo analiza los productos uno por uno y, para cada uno de ellos, intenta colocarlo en todos los contenedores donde exista espacio suficiente. Despues de cada asignación posible, se actualiza la ganancia parcial obtenida y se calcula la función límite de la nueva rama generada. La exploración continúa solamente si la rama todavía tiene posibilidades de superar la mejor solución encontrada hasta ese momento. Además, también se considera la posibilidad de no enviar el producto actual, generando así otra rama dentro del árbol de búsqueda.

            Cuando se alcanza una hoja del árbol, es decir, cuando ya se tomaron decisiones para todos los productos, se calcula la penalización total asociada al espacio libre de los contenedores utilizados y se obtiene la ganancia final de la configuración construida. Finalmente, si esta ganancia resulta mayor que la mejor solución conocida, la solución se actualiza.

        \subsubsection{Función costo}

            Representa la ganancia obtenida para un estado dado.

            \[
            G = \sum v_i - a \cdot \sum (C - U_j)
            \]

            Donde $U_j$ es el espacio utilizado en el contenedor ``\textit{j}''

        \subsubsection{Función límite}

            Permite estimar el máximo beneficio posible que podría alcanzarse.


            \[
            cota\_superior = ganancia\_actual + \sum valores\_restantes
            \]

            Esta función representa una estimación optimista, ya que considera que todos los productos restantes podrían agregarse sin restricciones de capacidad.


            \[
            cota\_superior \leq mejor\_solucion
            \]

        \subsubsection{Complejidades}

            Para cada producto existen \(k+1\) decisiones posibles, por lo tanto, en el peor caso, el algoritmo puede recorrer:

            \[
            (k+1)^n
            \]

            Dando una complejidad temporal:

            \[
            T(n)=O((k+1)^n)
            \]


            Teniendo en cuenta que la profundidad máxima del arbol es igual a la cantidad de productos \(n\), y que las estructuras auxiliares que usamos dependen de la cantidad de contenedores \(k\), la complejidad espacial total del algoritmo queda dada por:

            \[
            E(n)=O(n+k)
            \]

        \subsubsection{Ejemplo de ejecución}
            Teniendo el siguiente conjunto de productos:

\begin{verbatim}
    productos = [(2,10), (5,10), (7,20)]
    k = 2
    C = 8
    a = 5
\end{verbatim}

            Asignamos los productos \(0\) y \(1\) al primer contenedor, mientras que el producto \(2\) lo colocamos en el segundo contenedor:
\begin{verbatim}
    Contenedor 1 = [0,1]
    Contenedor 2 = [2]
\end{verbatim}

            De esta manera, los productos del primer contenedor ocupan \(2+5=7\), mientras que la del segundo contenedor \(7\). Por lo tanto, ambos contenedores tienen \(1\) unidad de espacio libre.

            La penalización total aplicada sobre el espacio no usado es:
            \[
            5(1+1)=10
            \]

            Por otro lado, el valor total obtenido por enviar los productos es:
            \[
            10+10+20=40
            \]

            Finalmente, la ganancia total es de:
            \[
            40-10=30
            \]

            Por lo tanto, el algoritmo retorna:
            \[
            (30, [[0,1], [2]])
            \]

        \subsubsection{Pseudocódigo}

\begin{lstlisting}[style=pseudo]
main(productos, k, C, a):

    mejor_ganancia = -infinito
    mejor_solucion = []
    contenedores = arreglo de k listas vacias
    ocupacion = arreglo de k ceros

    branch_and_bound(i = 0, productos, contenedores,
                    ocupacion, ganancia_actual = 0)

    retornar mejor_ganancia, mejor_solucion
\end{lstlisting}


{\small
\begin{lstlisting}[style=pseudo]
branch_and_bound(i, productos, contenedores, ocupacion, ganancia_actual):

    if(i == cantidad_productos):

        calcular penalizacion total

        ganancia_final = ganancia_actual - penalizacion

        if(ganancia_final > mejor_ganancia):

            actualizar mejor_ganancia
            guardar copia de contenedores

        retornar

    cota = ganancia_actual + suma_valores_restantes(i)

    if(cota <= mejor_ganancia):

        podar rama
        retornar

    cantidad, valor = productos[i]
    
    para cada contenedor j:
    
    if(ocupacion[j] + cantidad <= C):

            agregar producto i al contenedor j

            ocupacion[j] += cantidad

            branch_and_bound(i + 1, productos, contenedores,
                            ocupacion, ganancia_actual + valor)

            deshacer cambios

    explorar no enviar el producto actual
\end{lstlisting}

}

            Para este problema se utilizo la técnica de \textit{Branch \& Bound} para resolverlo. Se partió de una solución de búsqueda exhaustiva tradicional que garantiza encontrar el óptimo pero requiere recorrer todas las configuraciones posibles. Sobre esta idea se construyó una mejora mediante el uso de funciones límite y podas, permitiendo descartar ramas que no pueden superar la mejor solución encontrada.

    \subsection{Los centros de atención}

        La realización de esta sección no fue posible por constricciones de tiempo y solapamiento de responsabilidades de los integrantes del grupo. Para admitir una mejor reflexión sobre el ejercicio a realizarse con un mayor tiempo de trabajo se decidió suspender la realización de este ejercicio a una versión posterior (es decir, en el periodo de reentrega) de este informe.

    \section{Correcciones en la segunda versión}

        En la presente reentrega del trabajo práctico se han incorporado diversas modificaciones con el fin de subsanar las observaciones señaladas en la corrección inicial. Principalmente, en el apartado de Redes de Flujo, se reformuló la propuesta de solución de la Sección 2.1.2 para modelar la red considerando la bidireccionalidad intrínseca de las relaciones, lo cual conllevó la consecuente actualización del pseudocódigo para garantizar la viabilidad algorítmica. 
        
        Asimismo, se robusteció el análisis teórico profundizando la evaluación de la complejidad espacial de las estructuras de datos utilizadas. Por último, se reelaboró integralmente el ejemplo gráfico de la Sección 2.1.4: se detalló el paso a paso de la saturación de la red mediante la búsqueda de flujo máximo y se corrigió la construcción del grafo residual, reflejando de manera precisa la actualización de capacidades directas e inversas de forma previa a la determinación del corte mínimo.

\section{Conclusión}

    A partir de este trabajo de investigación y de la puesta en práctica de los conocimientos obtenidos en esta etapa avanzada de la Teoría de Algoritmos, se ha logrado profundizar en el análisis de problemas de optimización de alta complejidad. El estudio de las Redes de Flujo permitió comprender la modelización de sistemas con restricciones de capacidad, mientras que el abordaje de la Fuerza Bruta y las Clases de Complejidad brindó una perspectiva integral sobre los límites de la eficiencia computacional y la distinción entre problemas tratables e intratables.

    Asimismo, se enriqueció la labor realizada mediante una investigación exhaustiva que integró tanto el material bibliográfico de la cátedra como el análisis de publicaciones técnicas y recursos académicos complementarios. Esta base teórica fue fundamental para entender no solo el funcionamiento de los algoritmos, sino también las implicancias de las reducciones entre problemas y la jerarquía de complejidad que rige a la informática moderna.

    Finalmente, la implementación en el lenguaje Python de las soluciones propuestas para redes de flujo y algoritmos de búsqueda exhaustiva resultó clave para validar los modelos teóricos. Este proceso permitió observar el comportamiento real de los algoritmos frente a diferentes volúmenes de datos y casos de prueba, consolidando la transición entre el diseño conceptual y la aplicación práctica de soluciones óptimas en entornos de ingeniería.

\section{Referencias}

    \begin{enumerate}
        \item J. Kleinberg, E. Tardos, Algorithm Design, Addison Wesley (2006). [KT]
        \item T. Cormen, C. Leiserson, R. Rivest, C. Stein, Introduction to Algorithms (cuarta edición), MIT Press (2022). [CLRS]
        \item Victor Podberesky. Videos de Youtube del Canal vpode. [VPOD]
    \end{enumerate}

\end{document}
